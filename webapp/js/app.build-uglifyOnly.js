/*
 * This build will work the CSS files and minify each JS files but will not concatenate them
 */
({
  appDir: "../",
  dir: "../../build-02",
  baseUrl: "js/",
  paths: {
      "jquery": "empty:"
  }
})